{
  "name": "ai-code-companion",
  "version": "0.1.1",
  "private": true,
  "description": "AI Code Companion \u2014 Tauri v2 + NestJS + MCP + LangChain + ChromaDB desktop app for chatting with your codebase.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "app",
    "backend"
  ],
  "scripts": {
    "audit": "npm audit --audit-level=high",
    "build": "npm run build --workspaces --if-present",
    "build:tauri": "npm run package --workspace @ai-code-companion/backend && npm run tauri:build --workspace app",
    "check": "npm-run-all --sequential format:check lint typecheck knip deps test",
    "clean": "npm run clean --workspaces --if-present",
    "deps": "depcruise app/src backend/src packages/contracts/src scripts",
    "dev": "npm run build --workspace @ai-code-companion/contracts && npm-run-all --parallel dev:contracts dev:backend dev:app",
    "dev:app": "npm run dev --workspace app",
    "dev:backend": "npm run dev --workspace backend",
    "dev:contracts": "npm run dev --workspace @ai-code-companion/contracts",
    "dev:tauri": "npm run build --workspace @ai-code-companion/contracts && npm run package:if-missing --workspace @ai-code-companion/backend && npm run tauri:dev --workspace app",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "icons": "npm run tauri -w app -- icon app/src-tauri/icon.svg",
    "knip": "knip",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "mcp": "npm run mcp --workspace backend",
    "mutation": "npm-run-all --sequential mutation:contracts mutation:backend mutation:app",
    "mutation:app": "npm run mutation --workspace app",
    "mutation:backend": "npm run mutation --workspace backend",
    "mutation:contracts": "npm run mutation --workspace @ai-code-companion/contracts",
    "prepare": "husky",
    "rust:fmt": "cargo fmt --manifest-path app/src-tauri/Cargo.toml --all -- --check",
    "rust:lint": "cargo clippy --manifest-path app/src-tauri/Cargo.toml --all-targets -- -D warnings",
    "size": "npm run build --workspace app && node scripts/check-bundle-size.mjs",
    "test": "npm run test --workspaces --if-present",
    "test:cov": "npm run test:cov --workspaces --if-present",
    "test:e2e": "npm run test:e2e --workspace app",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "updater:enable": "node scripts/enable-updater.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@stryker-mutator/core": "^10.0.0",
    "@stryker-mutator/vitest-runner": "^10.0.0",
    "@vitest/eslint-plugin": "^1.6.27",
    "dependency-cruiser": "^18.2.0",
    "eslint": "^9.39.5",
    "eslint-config-prettier": "^10.1.8",
    "eslint-import-resolver-typescript": "^4.4.5",
    "eslint-plugin-import-x": "^4.17.1",
    "eslint-plugin-jsx-a11y": "^6.10.2",
    "eslint-plugin-playwright": "^2.11.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-sonarjs": "^4.2.0",
    "eslint-plugin-unicorn": "^65.0.1",
    "globals": "^17.12.0",
    "husky": "^9.1.7",
    "knip": "^6.34.0",
    "lint-staged": "^17.4.1",
    "npm-run-all2": "^8.0.4",
    "prettier": "^3.9.6",
    "typescript": "5.7.3",
    "typescript-eslint": "^8.69.0"
  },
  "overrides": {
    "vite": "^7.3.6"
  }
}
